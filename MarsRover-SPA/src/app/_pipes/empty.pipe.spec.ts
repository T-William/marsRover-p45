/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EmptyPipe } from './empty.pipe';

describe('Pipe: Emptye', () => {
  it('create an instance', () => {
    let pipe = new EmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
